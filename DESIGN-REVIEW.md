# تحليل تصميم منصة "إجازة" — Design Review

> مراجعة تصميم `index.html` — منصة تأجير فلل وشاليهات ريف دمشق.
> الهدف: نقل الصفحة من "قالب لطيف" إلى "منتج حقيقي".

## الخلاصة (TL;DR)

البنية سليمة: في design tokens، radii متسقة، إيقاع أقسام واضح، وحتى `prefers-reduced-motion`.
لكن الصفحة تبدو "قالب جميل" لا "منتج فعلي" لأسباب محددة. أهم ثلاثة إصلاحات:

1. **أضف لون هوية واحد (accent)** واستخدمه في الأزرار الأساسية، الأسعار، والحالات النشطة.
2. **أصلح تايبوغرافيا العربية** (احذف كل `letter-spacing`، ارفع `line-height` وحجم الخط الأساسي).
3. **اجعل صندوق البحث يعمل فعلاً أو احذفه** — صندوق بحث ميت أكبر إشارة على أن المنتج غير حقيقي.

---

## 1. لا توجد هوية — اللوحة اللونية أحادية 100%

كل لون في التصميم ([tokens](index.html#L13-L22)) هو navy أو charcoal أو أبيض أو رمادي. هذا أكبر سبب يجعلها تبدو عامة: لا يوجد لون accent تملكه العلامة.

**الإصلاح:** لون واحد دافئ (تراكوتّا / زيتوني / ذهبي دمشقي) يُستخدم بحذر في ~5 أماكن فقط:
- زر البحث `.btn-icon`
- التبويب النشط `.filter-tab.active`
- الأسعار `.card-price`
- hover على الروابط
- عناصر `.eyebrow`

المرجع: [index.html:13-22](index.html#L13-L22)

---

## 2. تُعامَل العربية كأنها إنجليزية — وأمر واحد معطوب فعلاً

| المشكلة | الموقع | الإصلاح |
|---|---|---|
| `letter-spacing: 0.14em` على `.eyebrow` | [index.html:56](index.html#L56) | **احذفه** — العربية خط متصل، والتباعد يفكّ الحروف الموصولة |
| `letter-spacing: -0.01em` على العناوين | [index.html:75](index.html#L75) | احذفه |
| `line-height: 1.05` على العناوين | [index.html:74](index.html#L74) | ارفعه إلى ~1.3–1.4 (العربية أطول صعوداً وهبوطاً وفيها تشكيل) |
| حجم النص الأساسي `15px` | [index.html:78](index.html#L78) | ارفعه إلى 16–17px (العربية تُقرأ أصغر بنفس الحجم) |
| `text-transform: uppercase` | [index.html:57](index.html#L57) | بلا أثر في العربية — استبدله بلون accent أو زيادة وزن بسيطة |

> **الأهم:** `letter-spacing` على نص عربي عيب مرئي فوري — يظهر فجوات بين الحروف المتصلة. الـ eyebrows تحتوي نصاً عربياً، فهي متضررة الآن.

---

## 3. صندوق البحث يبدو حقيقياً لكنه وهمي — وهذا أسوأ من غيابه

التواريخ وعدد الضيوف عبارة عن `<select>` بخيارات مثل "بعد ٣ ليالٍ"، وزر البحث غير موصول بشيء.
المستخدم سيضغط، لا يحدث شيء، وتنهار الثقة عند الـ hero مباشرة.

**خياران صادقان:**
- **(أ)** اربطه فعلياً ليصفّي/يمرّر إلى شبكة الفلل (حتى ببيانات وهمية).
- **(ب)** استبدله بـ CTA واحد قوي ("تصفّح الفلل" / واتساب) حتى يصبح البحث حقيقياً.

المرجع: [index.html:542-564](index.html#L542-L564)

---

## 4. الصور تقول "كاليفورنيا" لا "ريف دمشق"

صور Unsplash بيوت زجاجية غربية حديثة. حتى قبل تصويرك الخاص، اختر stock يطابق عمارة المنطقة: حجر، جبال، حدائق، ضوء دافئ.

**مشاكل تقنية إضافية:** لا `loading="lazy"`، لا `width`/`height` (فتقفز الصفحة أثناء التحميل)، وروابط hotlink قد تنكسر في الإنتاج.

---

## 5. الـ RTL نصف مكتمل

الصفحة `dir="rtl"` لكن الـ CSS يستخدم خصائص فيزيائية:
- `border-left` على `.search-field` ([index.html:209](index.html#L209))
- بينما breakpoint الموبايل يعيد ضبط `border-right` ([index.html:505](index.html#L505)) — وهما لا يشيران لنفس الحافة.

**الإصلاح:** استخدم الخصائص المنطقية في كل مكان:
`border-inline-start` · `margin-inline-end` · `padding-inline`

---

## 6. لمسات بمستوى المنتج مفقودة

صغيرة منفردة، لكنها مجتمعة تفصل "demo" عن "product":

- **حالات التركيز (`:focus-visible`)**: hover في كل مكان، صفر focus styles — مستخدم لوحة المفاتيح يضيع.
- **هيدر ثابت (sticky)**: الهيدر `position:absolute` ويختفي عند التمرير ([index.html:115](index.html#L115)). اجعله sticky بخلفية تظهر تدريجياً بعد الـ hero.
- **قائمة الموبايل**: inline styles محقونة من JS ([index.html:807-819](index.html#L807-L819)) — انقلها إلى toggle عبر CSS class، وأضف إغلاقاً عند الضغط على رابط.
- **Scroll reveal**: fade-up خفيف على الأقسام/الكروت عبر `IntersectionObserver` (مع احترام reduced-motion) يرفع الجودة المُدرَكة كثيراً بـ ~15 سطر.
- **أساسيات `<head>`**: لا favicon، لا `meta description`، لا Open Graph. عند مشاركة الرابط على واتساب (قناتك الأساسية!) يظهر كرابط عارٍ بلا صورة معاينة — خسارة حقيقية لمنتج يعتمد واتساب.
- **صدق حالة الفراغ**: شارة "مثال توضيحي" مكررة 3 مرات — صمّم هذه الحالة عمداً (كرت باهت قليلاً مثلاً) بدل 3 شارات متطابقة تبدو كخطأ.

---

## أولويات التنفيذ

**لو نفّذت 3 أشياء فقط:**
1. أضف لون accent واحد وطبّقه على CTAs / الأسعار / الحالات النشطة.
2. أصلح تايبوغرافيا العربية (احذف كل letter-spacing، ارفع line-height والحجم الأساسي).
3. اجعل صندوق البحث يعمل أو يختفي.

هذه الثلاثة تغيّر الانطباع الأول أكثر من كل ما عداها مجتمعاً.

---

*مرجع الملف: [index.html](index.html)*
